# Short health/status requests must not include the default timeout snippet:
# Nginx rejects duplicate proxy_*_timeout directives in the same location.
include /etc/nginx/snippets/trend-exploring-proxy-base.conf;
proxy_connect_timeout 2s;
proxy_read_timeout 5s;
proxy_send_timeout 5s;
