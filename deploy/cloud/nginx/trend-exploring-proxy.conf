# Default reverse-proxy policy for application/provider requests. The
# transport and forwarded-request headers live in the base snippet so the
# short and login variants can select their own timeout values safely.
include /etc/nginx/snippets/trend-exploring-proxy-base.conf;
proxy_connect_timeout 5s;
proxy_read_timeout 60s;
proxy_send_timeout 60s;
