# Login/recovery requests use a bounded but longer timeout than liveness while
# sharing only the transport/header policy (not the default timeout snippet).
include /etc/nginx/snippets/trend-exploring-proxy-base.conf;
proxy_connect_timeout 5s;
proxy_read_timeout 10s;
proxy_send_timeout 10s;
